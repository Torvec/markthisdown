// !This doesn't work like I thought it would, will come back to it another time

// function getById(id) {
//   return document.getElementById(id);
// }

// getById("electronVer").innerText = `Electron: v${aboutInfo.electron()}`;
// getById("chromeVer").innerText = `Chromium: v${aboutInfo.chrome()}`;
// getById("nodeVer").innerText = `Node: v${aboutInfo.node()}`;
// getById("v8Ver").innerText = `V8: v${aboutInfo.v8()}`;
// const osType = aboutInfo.osType();
// const osArch = aboutInfo.osArch();
// const osRelease = aboutInfo.osRelease();
// getById("osInfo").innerText = `OS: ${osType} ${osArch} ${osRelease}`;
